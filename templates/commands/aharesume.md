Resume context from previous Ahahooh sessions. Call the `get_resume_context` MCP tool once, then output the result directly with minimal formatting. Do NOT add summaries, analysis, or extra commentary. Just present what the tool returns.

$ARGUMENTS
